<?xml version="1.0" encoding="UTF-8"?>
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Auth0 – DonorAtlas Users</label>

    <!-- VALID enum value -->
    <authenticationProtocol>OAuth</authenticationProtocol>

    <!-- Variant is optional; BrowserFlow is the implicit default -->
    <authenticationProtocolVariant>BrowserFlow</authenticationProtocolVariant>

    <!-- Tell SF that each user must authorise via the Auth Provider -->
    <externalCredentialParameters>
        <parameterName>AuthProvider</parameterName>
        <parameterType>AuthProvider</parameterType>
        <parameterValue>Auth0_OIDC</parameterValue>
        <sequenceNumber>1</sequenceNumber>
    </externalCredentialParameters>

    <!-- Per-user seats -->
    <principalType>PerUser</principalType>
</ExternalCredential>